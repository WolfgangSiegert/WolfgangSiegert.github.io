# WolfgangSiegert.github.io

Statische Hauptseite für die eigene Domain. Das Portfolio bleibt als separates GitHub-Pages-Projekt unter `/portfolio/` erreichbar.

Die Veröffentlichung ist absichtlich noch nicht aktiviert. Nach der Freigabe: Repository öffentlich schalten, unter Settings → Pages `Deploy from a branch`, `main` und `/ (root)` wählen und anschließend die eigene Domain eintragen.
